# 톤필드 장력 시각화 시스템 - 최종 기술 분석 문서 v2.5

## 📋 문서 개요

**최종 업데이트**: 2025-11-16 (v2.5 - 중앙 돔 쉐이딩 + 내부 장력선)
**대상 컴포넌트**: `TonefieldTensionDiagram.tsx`
**목적**: 핸드팬 톤필드의 물리적 장력 분포를 다층 SVG 시각화로 표현
**기술 스택**: React 19, TypeScript, SVG, Tailwind CSS
**핵심 설계 원칙**: **"타원 경계선은 순수 타원형 유지, 내부 효과로만 장력 표현"**

---

## 🎯 시스템 목표 및 물리적 배경

### 물리적 원리
핸드팬의 톤필드는 금속 표면에 딤플을 타격하여 형성된 타원형 영역입니다.
- **중심 (딤플)**: 고정점 역할, 장력의 기준점
- **림 (외곽 경계)**: 장력이 최대로 집중되는 지점
- **중간 영역**: 중심에서 림으로 갈수록 장력이 증가

### 시각화 목표
1. **장력 분포 레벨** - 림 집중 등고선으로 표현 (88-98% 구간)
2. **주축 장력 방향** - Octave/Tonic/Fifth 4방향 주요 축 강조
3. **중앙 돔 입체감** - 방사형 돔 쉐이딩 (중앙 볼록 + 림 눌림)
4. **막의 결 표현** - 내부 장력선 (림→중앙 반쯤, 12개)
5. **테두리 압박 효과** - 외곽 인워드 섀도우 (림 85-100%)
6. **장력 경계 명확화** - 장력 벨트 (림 94-100% 하이라이트)
7. **앵커 고정점 구조** - 4방향 림 틱
8. **순수 타원 경계** - 변형 없는 완벽한 타원형 림
9. **외부 응력 전파** - 리플 엣지 (미세 파동)

---

## 🏗️ 최종 레이어 구조

### 활성화된 레이어 (11개)

```
렌더링 순서 (Z-Index: 하위 → 상위)

0. [Structure] 바깥 타원              완벽한 타원형 경계선
1. [Effect] 리플 엣지                103%, 106% 점선 타원
2. [Base] 기본 방사형 그라데이션      중심→외곽 그라데이션
3. [Overlay] 중앙 돔 쉐이딩 ⚡⚡       방사형 (중앙 밝음 → 림 어두움)
4. [Lines] 등고선 ⚡⚡                3개, 림 집중 [0.88,0.94,0.98]
5. [Lines] 주축 장력선 🔄            4개, Octave/Tonic/Fifth만
6. [Lines] 내부 장력선 ⚡            12개, 림(95%)→중앙(40%) 반쯤
7. [Shadow] 인워드 섀도우 ⚡          85%-100%, opacity 0.35
8. [Highlight] 장력 벨트 ⚡⚡          94%-98%, 안쪽 경계 강조 (0.65)
9. [Highlight] 림 눌림 링 ⚡          96%-100%, 어두운 쉐도우 (0.35)
10. [Structure] 림 앵커 틱 🔄         4개 틱, 주요 방향만
11. [Effect] 외부 방사형 화살표 ⚡    360개, 외부 응력 전파 (v2.5 재활성화)
```

**⚡ = 파라미터 최적화** | **⚡⚡ = 대폭 변경** | **🔄 = 기능 변경**

### 비활성화된 요소

```
❌ 장력 림 (Tension Rim)             앵커 방향 bulge 변형
   이유: 타원 경계선 자체 변형은 물리적 의미 불명확
   결정: 경계선은 순수 타원형 유지, 내부 효과로만 장력 표현
   위치: 라인 382 (enabled: false)

❌ 벡터 필드 그라데이션              24개 섹터 방향성 패턴
   이유: 시각 정보 과다, 림 집중 약화
   위치: 라인 284 (enabled: false)
```

**v2.5 재활성화**:
- ✅ 외부 방사형 화살표 (External Radial Arrows): v2.1-v2.4 비활성화 → v2.5 재활성화

---

## 📊 효과별 상세 파라미터 설정값

### 1️⃣ 등고선 (Contour Lines) - 극단적 림 집중

**활성화 상태**: ✅ 활성화 (`enabled: true`)
**코드 위치**: 라인 209-227

**설정값**:
```typescript
const CONTOUR_CONFIG = {
  enabled: true,

  // 극단적 림 집중 비율 (88-98% 구간만)
  ratios: [0.88, 0.94, 0.98],         // v2.1: [0.70,0.82,0.90,0.97] → 3개로 축소

  // 색상 및 스타일
  color: "#2f9c7c",                   // 진한 청록색 (v2.1: #3bb18f)
  baseOpacity: 0.20,                  // v2.1: 0.18
  opacityStep: 0.06,                  // 안쪽으로 투명도 감소

  // 선 두께
  outerStrokeWidth: 0.9,              // 유지
  innerStrokeWidth: 0.7,              // v2.1: 0.5 → 더 굵게
};
```

**효과**:
- 중앙 0-88%: 완전히 깨끗 (기존 0-70%)
- 림 주변 88-98%: 3개 등고선 극도로 촘촘
- **"림 10% 구간에 모든 정보 집중"** 극대화

**렌더링**: 4개 `<ellipse>` 요소 (라인 845-861)

**물리적 의미**: 등압선과 같이 동일한 장력 분포를 표현

---

### 2️⃣ 주축 장력선 (Primary Tension Axes) - 4방향만 강조 🔄

**활성화 상태**: ✅ 활성화 (`enabled: true`)
**코드 위치**: 라인 231-253 (CONFIG), 1052-1076 (렌더링)

**설정값**:
```typescript
const TENSION_AXIS_CONFIG = {
  enabled: true,

  // 주축 각도 (Octave/Tonic/Fifth 4방향)
  angles: [0, π/2, π, 3π/2],          // 우/하/좌/상

  // 위치 범위
  innerRatio: 0.35,                   // 딤플 근처에서 시작
  outerRatio: 0.98,                   // 림 바로 안쪽까지

  // 스타일
  strokeWidth: 1.1,                   // 선 두께
  color: "#1b6a57",                   // 진한 청록색
  opacity: 0.7,                       // 투명도
};
```

**변경 이유** (v2.1 → v2.2):
- v2.1: 24개 전방향 방사형 장력선
- v2.2: 4개 주축만 표시
- **근거**: 장력 림(Tension Rim)이 이미 360° 변형을 표현하므로, 선은 주요 방향만 강조

**효과**:
- Octave/Tonic/Fifth 방향의 주요 장력 축만 표현
- 시각적 복잡도 감소 (24개 → 4개)
- 장력 림의 bulge와 방향 일치
- **"핵심 방향만 강조"** 전략

**물리적 의미**:
- 핸드팬의 주요 음정 방향 (Octave/Tonic/Fifth)
- 실제 타격 시 주요 장력 전달 경로

---

### 2-1️⃣ 내부 장력선 (Inner Stretch Lines) - "막의 결" 표현 (v2.5) ⚡

**활성화 상태**: ✅ 활성화 (`enabled: true`)
**코드 위치**: 라인 255-271 (CONFIG), 1073-1100 (렌더링)

**설정값**:
```typescript
const INNER_STRETCH_LINES_CONFIG = {
  enabled: true,

  // 선 개수 및 위치
  count: 12,                          // 360/12 = 30도 간격
  innerRatio: 0.40,                   // 중앙 40% 지점에서 끝
  outerRatio: 0.95,                   // 림 바로 안쪽에서 시작

  // 스타일
  strokeWidth: 0.6,                   // 매우 얇게
  opacity: 0.18,                      // 매우 옅게
  color: "#145c4a",                   // 진한 청록색
};
```

**디자인 의도** (v2.5 신규):
- **방향**: 림(95%) → 중앙(40%), 반쯤만 들어옴
- **효과**: "밖에서 잡아당겨 안쪽으로 긴장 걸린 실"처럼 보임
- **중앙 보존**: 선이 중앙까지 닿지 않아 돔 쉐이딩 깨끗하게 유지

**효과**:
- 12개 선 (30도 간격) → 균일한 "막의 결" 표현
- 림에서 시작 → 바깥에서 안쪽으로 당기는 뉘앙스
- 중앙 40%에서 멈춤 → 중앙 돔 영역 보호
- 매우 얇고 옅게 → 미묘한 질감, 과하지 않음

**물리적 의미**:
- 장력으로 늘어난 금속 막의 섬유질 구조
- 림에서 중앙으로 전달되는 장력 방향성
- 주축 장력선(4개)과 보완 관계

---

### 3️⃣ 중앙 돔 쉐이딩 (Central Dome Shading) - 방사형 볼록 효과 (v2.5) ⚡⚡

**활성화 상태**: ✅ 활성화 (`enabled: true`)
**코드 위치**: 라인 273-300 (CONFIG), 694-703 (그라데이션), 1002-1011 (렌더링)

**설정값**:
```typescript
// MEMBRANE_SHADING_CONFIG (기존 이름 유지, 용도 변경)
const MEMBRANE_SHADING_CONFIG = {
  enabled: true,
  // (기존 파라미터들은 사용 안 함, 그라디언트에서 직접 정의)
};

// 그라디언트 정의 (radialGradient)
<radialGradient id="membrane-dome" cx="50%" cy="50%" r="60%">
  <stop offset="0%" stopColor="white" stopOpacity="0.45" />
  <stop offset="25%" stopColor="white" stopOpacity="0.30" />
  <stop offset="55%" stopColor="#0f172a" stopOpacity="0.12" />
  <stop offset="80%" stopColor="#0f172a" stopOpacity="0.22" />
  <stop offset="100%" stopColor="#0f172a" stopOpacity="0.30" />
</radialGradient>
```

**그라데이션 구조** (radial, 5-stop):
```
중앙 0%:     white opacity 0.45  (돔 정점, 강한 하이라이트)
중앙 25%:    white opacity 0.30  (돔 경사)
중간 55%:    #0f172a opacity 0.12  (돔 받침 링, 평탄 전환)
림 80%:      #0f172a opacity 0.22  (눌림 시작)
림 100%:     #0f172a opacity 0.30  (최대 눌림)
```

**디자인 변경** (v2.4 → v2.5):
- **방향**: linearGradient (수직) → radialGradient (방사형)
- **효과**: 전체적 입체감 → **중앙 돔 + 림 눌림** 명확화
- **물리적 의미**: "중앙에서 펴지는 힘" 시각화

**효과**:
- 0-25%: 밝은 돔 → 중앙이 위로 볼록하게 느껴짐
- 25-55%: 평탄 전환 → 돔에서 평면으로 자연스러운 연결
- 55-100%: 점점 어두워져 → 림 쪽이 눌린 느낌
- **핵심**: "중앙 돔이 안쪽에서 바깥으로 민다, 림은 그 힘을 받아 눌려 있다"

**물리적 의미**: 장력으로 당겨진 금속 표면의 곡률과 빛 반사

---

### 4️⃣ 벡터 필드 그라데이션 (Vector Field Gradient) - ❌ 비활성화

**활성화 상태**: ❌ 비활성화 (`enabled: false`)
**코드 위치**: 라인 281-299 (CONFIG)

**설정값**:
```typescript
const VECTOR_FIELD_CONFIG = {
  enabled: false,                     // ❌ v2.2에서 비활성화
  stripeCount: 24,                    // 24개 섹터
  stripeIntensity: 0.04,              // 4% 밝기 변조

  baseColor: "#86efac",
  brightColor: "#a7f3c0",             // 밝은 섹터
  darkColor: "#6ee7a5",               // 어두운 섹터
  baseOpacity: 0.4,
};
```

**비활성화 이유** (v2.2):
- **시각 정보 과다**: 24개 섹터 패턴이 등고선/장력선과 충돌
- **림 집중 약화**: 전방향 패턴이 주축 강조 전략과 불일치
- **대체 요소**: 장력 림(Tension Rim)이 4방향 변형으로 방향성 표현

**원래 효과** (v2.1):
- 24개 섹터가 밝기 교대
- 장력이 방향성을 가지고 "진동"하는 느낌
- 벡터 필드 패턴

**v2.2 설계 결정**:
- 시각적 단순화 우선
- 림 변형(bulge)으로 방향성 표현

---

### 5️⃣ 외곽 인워드 섀도우 (Inward Shadow) - 림 집중 강화

**활성화 상태**: ✅ 활성화 (`enabled: true`)
**코드 위치**: 라인 302-316 (CONFIG), 727-751 (그라데이션)

**설정값**:
```typescript
const INWARD_SHADOW_CONFIG = {
  enabled: true,

  // 범위 설정 (바깥 15%에만 집중)
  startOffset: 0.85,                  // v2.1: 0.55 → 림 근처만
  endOffset: 1.0,

  // 색상 및 투명도
  color: "#1f5b52",                   // Dark teal
  startOpacity: 0.0,                  // 85% 지점: 투명
  endOpacity: 0.35,                   // v2.1: 0.30 → 더 진하게
};
```

**그라데이션**:
```typescript
<radialGradient id="inward-shadow-gradient">
  <stop offset="0%"  stopOpacity={0.0}  />   // 0-85% 완전 투명
  <stop offset="85%" stopOpacity={0.0}  />   // 85% 지점
  <stop offset="100%" stopOpacity={0.35} />  // 림 쪽 진한 그림자
</radialGradient>
```

**효과**:
- 85%-100% 영역에만 그림자 (v2.1: 55%-100%)
- **극단적 림 집중**: 바깥 15%에만 깊이 효과
- 림 주변 **"눌림"** 효과 극대화
- 외곽에서 안쪽으로 말려드는 장력 명확

**물리적 의미**: 림에서 팽팽하게 당겨져 안쪽으로 급격히 휘어지는 금속 표면

---

### 6️⃣ 장력 벨트 (Tension Belt) - 🆕 신규 레이어

**활성화 상태**: ✅ 활성화 (`enabled: true`)
**코드 위치**: 라인 282-303 (CONFIG), 637-667 (그라데이션), 948-957 (렌더링)

**설정값**:
```typescript
const TENSION_BELT_CONFIG = {
  enabled: true,

  // 위치
  insetOffset: 6,                     // 림에서 안쪽 6px

  // 그라데이션 범위
  innerStartOffset: 0.0,              // 내부 투명
  innerMidOffset: 0.75,               // 75%까지 투명
  beltStartOffset: 0.90,              // 90%에서 벨트 시작
  beltEndOffset: 1.0,                 // 100%에서 끝

  // 색상 및 투명도
  innerColor: "#ffffff",
  beltColor: "#ffffff",
  innerOpacity: 0.0,
  beltPeakOpacity: 0.55,              // 90% 지점 최대
  beltEndOpacity: 0.0,                // 100% 지점 사라짐
};
```

**그라데이션 구조**:
```
0%:    opacity 0.0   (내부 완전 투명)
75%:   opacity 0.0   (계속 투명)
90%:   opacity 0.55  (급격히 밝아짐) ← 벨트 하이라이트
100%:  opacity 0.0   (림에서 사라짐)
```

**효과**:
- 림 직전(90%)에서 밝은 하이라이트 띠
- **"여기까지가 장력 경계"** 명확 표현
- 드럼 헤드 bearing edge 하이라이트 효과
- 톤필드가 림에 단단히 고정되고 극도로 팽팽한 느낌

**물리적 의미**: 막이 림에 고정되는 경계 영역

---

### 7️⃣ 림 앵커 틱 (Rim Anchor Ticks) - 4방향만 🔄

**활성화 상태**: ✅ 활성화 (`enabled: true`)
**코드 위치**: 라인 365-380 (CONFIG), 1111-1133 (렌더링)

**설정값**:
```typescript
const RIM_ANCHOR_CONFIG = {
  enabled: true,

  // 틱 설정 (주요 방향만)
  count: 4,                           // v2.1: 24 → 4개로 축소
  innerRatio: 0.98,                   // 림 안쪽
  outerRatio: 1.02,                   // 림 바깥쪽

  // 색상 및 스타일
  stroke: "#145c4a",                  // 진한 청록색
  strokeWidth: 0.9,
  opacity: 0.7,
};
```

**렌더링 로직**:
```typescript
// 4방향 (Octave/Tonic/Fifth)
const angle = (i / 4) * 2 * Math.PI;

// 림 바깥(1.02)에서 시작
const sx = cx + Math.cos(angle) * rx * 1.02;
const sy = adjustedCy + Math.sin(angle) * ry * 1.02;

// 림 안쪽(0.98)으로 향함
const ex = cx + Math.cos(angle) * rx * 0.98;
const ey = adjustedCy + Math.sin(angle) * ry * 0.98;
```

**변경 이유** (v2.1 → v2.2):
- v2.1: 24개 틱 (전방향 균등 분포)
- v2.2: 4개 틱 (주요 방향만)
- **근거**: 장력 림(Tension Rim)이 이미 360° 고정점 효과를 표현

**효과**:
- Octave/Tonic/Fifth 방향만 틱 표시
- 장력 축 강조선과 방향 일치
- 시각적 단순화 (24개 → 4개)
- **"핵심만 남기기"** 전략

**물리적 의미**: 주요 음정 방향의 고정점 (실제 타격 지점)

---

### 8️⃣ ❌ 장력 림 (Tension Rim) - 비활성화

**활성화 상태**: ❌ 비활성화 (`enabled: false`)
**코드 위치**: 라인 382-401 (CONFIG)

**설정값**:
```typescript
const TENSION_RIM_CONFIG = {
  enabled: false,                     // ❌ 비활성화 (v2.4)

  // 경계선 변형은 사용하지 않음
  bulgeAmplitude: -0.04,              // (미사용)
  segments: 220,                      // (미사용)
  anchorAngles: [0, π/2, π, 3π/2],    // (미사용)
};
```

**비활성화 이유** (v2.4):
- **타원 경계선은 순수 타원형 유지**
- 경계선 자체의 변형(bulge)은 물리적 의미가 불명확
- 장력 효과는 **내부 레이어**로만 표현:
  - 등고선 (림 집중)
  - 주축 장력선 (4방향)
  - 인워드 섀도우
  - 장력 벨트
  - 림 눌림 링
  - 림 앵커 틱

**렌더링**:
```typescript
// 항상 완벽한 타원형
<ellipse cx={cx} cy={cy} rx={rx} ry={ry}
         fill="none" stroke="#334155" />
```

**설계 철학** (v2.4):
- **경계선 순수성**: 타원형 경계는 변형하지 않음
- **내부 효과 집중**: 장력은 도너츠 영역 내부 효과로만 표현
- **명확한 구분**: 경계(타원) vs 내부 장력 효과

**변경 이력**:
- v2.2: 바깥 돌출 bulge (+12%) → "삐져나왔다" 오인
- v2.3: 안쪽 눌림 bulge (-4%) → "잡아당겼다" 직관적
- v2.4: ❌ **bulge 완전 제거** → 순수 타원형 유지

---

### 9️⃣ 림 눌림 링 (Rim Press Ring) - v2.5 재정의 ⚡

**활성화 상태**: ✅ 활성화 (`enabled: true` - TENSION_RIM_CONFIG)
**코드 위치**: 라인 758-762 (그라데이션), 1091-1099 (렌더링)

**설정값**:
```typescript
<radialGradient id="rim-press-ring" cx="50%" cy="50%" r="60%">
  <stop offset="96%" stopColor="#020617" stopOpacity="0.35" />
  <stop offset="100%" stopColor="#020617" stopOpacity="0.0" />
</radialGradient>
```

**디자인 변경** (v2.4 → v2.5):
- **v2.4**: 밝은 링 (`#ffffff` opacity 0.8) - 최대 장력 하이라이트
- **v2.5**: 어두운 쉐도우 (`#020617` opacity 0.35) - **"눌린 림"** 강조
- **이름**: Rim Stress Ring → **Rim Press Ring**

**효과**:
- 림 96-100% 사이의 **얇은 어두운 쉐도우**
- 림이 눌린 자리를 명확히 표현
- 중앙 돔 쉐이딩(밝음)과 대비되어 림의 눌림 강조
- 투명도 0.35에서 0.0으로 페이드아웃

**렌더링**:
```typescript
{TENSION_RIM_CONFIG.enabled && (
  <path d={tensionRimPath} fill="url(#rim-press-ring)" />
)}
```

**물리적 의미**:
- 림의 최외각 4% 구간 = 중앙 돔에 눌린 영역
- "중앙에서 펴지는 힘 vs 림에서 받는 힘"의 균형점
- 장력 벨트(밝음)와 림 눌림 링(어두움)의 대비로 경계 명확화

**장력 벨트와의 차이** (v2.5):
| 항목 | 장력 벨트 | 림 눌림 링 |
|------|----------|-------------|
| 구간 | 94-98% | 96-100% |
| 색상 | #ffffff (밝음) | #020617 (어두움) |
| 최대 opacity | 0.65 (94%) | 0.35 (96%) |
| 목적 | 안쪽 경계 강조 | 눌린 림 쉐도우 |
| 적용 경로 | 도너츠 path | 장력 림 path |

---

### 🔟 리플 엣지 (Ripple Edge)

**활성화 상태**: ✅ 활성화 (`enabled: true`)
**코드 위치**: 라인 318-338 (CONFIG), 950-967 (렌더링)

**설정값**:
```typescript
const RIPPLE_EDGE_CONFIG = {
  enabled: true,                      // ✅ 활성화

  layers: [
    { factor: 1.03, strokeWidth: 0.6, opacity: 0.40, dashArray: "3 4" },
    { factor: 1.06, strokeWidth: 0.4, opacity: 0.28, dashArray: "1.5 4.5" },
  ],

  color: "#7fd5b6",
  useBlur: true,
  blurAmount: 0.8,
};
```

**효과**:
- 경계 밖 3%, 6%에 점선 타원
- 응력이 바깥으로 전파되는 미세 파동

**물리적 의미**: 금속 표면의 응력선 (stress line)

---

### 1️⃣1️⃣ 외부 방사형 화살표 (External Radial Arrows) - v2.5 재활성화 ⚡

**활성화 상태**: ✅ 활성화 (v2.5 재활성화)
**코드 위치**: 라인 1237-1246

**렌더링 코드** (v2.5):
```typescript
{/* Radial tension arrows: 외부 방사형 화살표 (v2.5 재활성화) */}
{dirs.map((t, i) => {
  const { shaft, head } = arrowPath(t);
  return (
    <g key={i}>
      <path d={shaft} stroke={color} strokeWidth={2} fill="none" />
      <path d={head} stroke={color} strokeWidth={2} fill="none" />
    </g>
  );
})}
```

**설정값**:
- 개수: 360개 (디렉션 배열 기반)
- 위치: 타원 바깥쪽 (외부 방사형)
- 색상: `color` 변수 (파란-초록 계열)
- 선 두께: 2px

**활성화 변경 이력**:
- **v2.1 이전**: ✅ 활성화 (24개 외부 화살표)
- **v2.1-v2.4**: ❌ 비활성화 (`false &&` 조건)
  - 이유: "톤필드를 당기는 느낌 없음", "밀어내는" 오인 가능
- **v2.5**: ✅ 재활성화 (조건 제거)
  - 이유: 외부 응력 전파 및 방향성 표현 재도입

**효과** (v2.5):
- 타원 바깥쪽으로 방사형 화살표 표시
- 톤필드에서 외부로 전파되는 에너지 시각화
- 리플 엣지(파동)와 조합하여 응력 전파 효과 강화

**물리적 의미**: 타격 시 외부로 전파되는 진동 에너지와 응력선

---

## 🎨 색상 팔레트 및 시각적 계층

### 색상 체계

| 레이어 | 색상 코드 | RGB | 용도 |
|--------|-----------|-----|------|
| 등고선 | `#3bb18f` | (59, 177, 143) | 장력 레벨 구분 |
| 장력선 | `#2bb5a0` | (43, 181, 160) | 방사형 방향 |
| 벡터 필드 (밝음) | `#a7f3c0` | (167, 243, 192) | 힘의 장 하이라이트 |
| 벡터 필드 (어두움) | `#6ee7a5` | (110, 231, 165) | 힘의 장 베이스 |
| 인워드 섀도우 | `#1f5b52` | (31, 91, 82) | 림 그림자 |
| 장력 벨트 | `#ffffff` | (255, 255, 255) | 림 경계 하이라이트 |
| 림 앵커 | `#166f5b` | (22, 111, 91) | 고정점 강조 |
| 리플 엣지 | `#7fd5b6` | (127, 213, 182) | 외부 전파 |

**색상 선택 원칙**:
- Teal/Green 계열로 통일 (일관성)
- 밝기 차이로 레이어 구분
- 과도한 채도 회피 (은은함)

### 시각적 계층

```
[배경] 기본 그라데이션
  ↓
[방향성] 벡터 필드 (24개 섹터)
  ↓
[곡률] 멤브레인 쉐이딩 (상/하 대비)
  ↓
[레벨] 등고선 (림 집중)
  ↓
[방향] 장력선 (딤플→림, 24개)
  ↓
[깊이] 인워드 섀도우 (림 그림자)
  ↓
[경계] 장력 벨트 (90%-100% 밝은 띠) 🆕
  ↓
[구조] 림 앵커 (24개 고정 틱) 🆕
  ↓
[전파] 리플 엣지 (외부 파동)
```

---

## 📐 파라미터 변경 이력

### 최적화 비교표 (v1.0 → v2.2)

| 레이어 | 파라미터 | v1.0 | v2.1 | v2.2 | 변화 방향 | 효과 |
|--------|----------|------|------|------|----------|------|
| 등고선 | ratios | [0.85,0.70,0.55,0.40] | [0.70,0.82,0.90,0.97] | [0.88,0.94,0.98] | 림 극단 집중 | 88-98% 촘촘 |
| 등고선 | count | 4개 | 4개 | 3개 | 축소 | 단순화 |
| 등고선 | baseOpacity | 0.35 | 0.18 | 0.20 | 조정 | 가시성 |
| 등고선 | innerStrokeWidth | 0.6 | 0.5 | 0.7 | 증가 | 명확성 |
| 장력선 | 타입 | 방사형 24개 | 방사형 24개 | 주축 4개 | 핵심만 | 단순화 |
| 장력선 | 범위 | 전방향 | 전방향 | Octave/Tonic/Fifth | 방향 한정 | 주축 강조 |
| 멤브레인 | 단계 | 2단계 | 3단계 | 4단계 (중앙 추가) | 세분화 | 입체감 ↑ |
| 멤브레인 | topOpacity | 0.25 | 0.35 | 0.22 | 감소 | 자연스러움 |
| 멤브레인 | centerOpacity | - | - | 0.32 | 신규 | 중앙 강조 |
| 멤브레인 | bottomEndOpacity | 0.18 | 0.24 | 0.28 | 증가 | 대비 강화 |
| 벡터 필드 | enabled | true | true | false | 비활성화 | 시각 단순화 |
| 인워드 | startOffset | 0.65 | 0.55 | 0.85 | 림 집중 | 극단적 효과 |
| 인워드 | endOpacity | 0.22 | 0.30 | 0.35 | 증가 | 깊이 강화 |
| 장력 벨트 | beltPeakOffset | - | 0.90 | 0.97 | 조정 | 날카로운 링 |
| 장력 벨트 | beltPeakOpacity | - | 0.55 | 0.75 | 증가 | 명확성 |
| 림 앵커 | count | - | 24개 | 4개 | 핵심만 | 주축 일치 |
| 림 앵커 | 위치 | - | 안쪽 8px | 양방향 98-102% | 변경 | 구조 명확 |

### 신규 추가

**v2.2 - 림 고장력 링**:
| 레이어 | 핵심 파라미터 | 값 (v2.2-v2.4) | 효과 |
|--------|---------------|----------------|------|
| 림 고장력 링 | 구간 | 96-100% | 최대 장력 하이라이트 |
| 림 고장력 링 | peakOpacity | 0.8 | 강한 빛남 (밝은 링) |

**v2.5 - 림 눌림 링** (재정의):
| 레이어 | 핵심 파라미터 | 값 (v2.5) | 효과 |
|--------|---------------|-----------|------|
| 림 눌림 링 | 구간 | 96-100% | 눌린 림 쉐도우 |
| 림 눌림 링 | 색상 | #020617 (어두움) | 눌림 명확화 |
| 림 눌림 링 | opacity | 0.35 (96%) → 0.0 | 어두운 그림자 |

### 비활성화 (v2.4)

| 요소 | 개수/범위 | 비활성화 이유 | 대체 |
|------|----------|--------------|------|
| 장력 림 | 앵커 방향 bulge | 경계선 변형은 물리적 의미 불명확 | 내부 레이어 효과 |
| 벡터 필드 | 24개 섹터 | 시각 정보 과다, 림 집중 약화 | 주축 장력선 |
| 외부 화살표 | 24개 | 당기는 느낌 없음 | 주축 장력선 |

---

## 🔬 설계 원칙 및 의사결정

### 1. 물리적 정확성 우선

**원칙**:
- 팽팽한 장력 = 직선 (곡선 ❌)
- 장력 최대 지점 = 림 (중앙 ❌)
- 고정점 = 360° 균등 분포 (불규칙 ❌)

**적용 사례**:
- 장력선: Bezier curve → 직선 변경
- 장력선 그라데이션: 중심 진함 → 림 진함 역전
- 외부 화살표: 비활성화 (당기는 방향 아님)

### 2. 정보 밀도 집중 전략

**구간별 밀도**:
```
중앙 (0-70%)     │ 깨끗함      │ 0%   │ 매끈한 표면
중간 (70-90%)    │ 점진적 증가 │ 30%  │ 전환 영역
림 (90-100%)     │ 최대 집중   │ 100% │ 장력 집중
```

**구현 방법**:
- 등고선 4개를 70-97%에 배치
- 장력 벨트를 90-100%에 배치
- 인워드 섀도우를 55-100%로 확장
- 림 앵커를 정확히 림(100%) 위치에 배치

### 3. 시각적 계층 최적화

**레이어 순서 근거**:
1. 배경 (그라데이션) → 기본 톤 설정
2. 방향성 (벡터 필드) → 힘의 방향
3. 곡률 (멤브레인) → 3D 입체감
4. 레벨 (등고선) → 장력 분포
5. 방향 (장력선) → 명확한 방향성
6. 깊이 (그림자) → 3D 깊이
7. 경계 (벨트) → 하이라이트
8. 구조 (앵커) → 고정점
9. 전파 (리플) → 외부 효과

**상호작용 고려**:
- 그림자(어두움) + 벨트(밝음) = 경계 강조
- 장력선(방향) + 앵커(고정) = 시스템 통합
- 등고선(레벨) + 장력선(방향) = 분포+방향

---

## 🎓 의사결정 히스토리

### v2.1 주요 결정

#### 1. 등고선 림 집중 배치 (v2.1)
**날짜**: 2025-11-16
**문제**: [0.85, 0.70, 0.55, 0.40]은 정보가 고르게 분산
**결정**: [0.70, 0.82, 0.90, 0.97]로 림에 몰기
**효과**: 중앙 깨끗, 림 촘촘 → "가장자리 장력 집중" 명확

#### 2. 장력선 그라데이션 역전 (v2.1)
**날짜**: 2025-11-16
**문제**: 중심 진하게 → 외곽 약하게는 물리적 부정확
**결정**: 3-stop gradient (0.10 → 0.35 → 0.80) 림 강조
**효과**: 림을 "때리는" 느낌, 당겨진 가장자리 명확

#### 3. 장력 벨트 추가 (v2.1)
**날짜**: 2025-11-16
**목적**: 림 안쪽 밝은 띠로 경계 명확화
**근거**: 드럼 헤드 bearing edge 하이라이트
**효과**: "여기까지가 장력 경계" 명확 표현

#### 4. 림 앵커 추가 (v2.1)
**날짜**: 2025-11-16
**목적**: 24개 고정점으로 림 고정 구조 표현
**근거**: 드럼 헤드 lug 포인트 개념
**효과**: 장력선과 방향 일치 → "하나의 시스템"

#### 5. 외부 화살표 비활성화 (v2.1)
**날짜**: 2025-11-16
**문제**: 타원 외부 바깥쪽 화살표 24개
**피드백**: "톤필드를 당기는 느낌이 나지 않음"
**결정**: `false &&` 조건으로 비활성화
**효과**: 장력 집중 의도 명확, 혼란 제거

---

### v2.2 주요 결정

#### 6. 장력 림 (Tension Rim) 추가 (v2.2) 🔥
**날짜**: 2025-11-16
**목적**: 앵커 고정점에서 림이 바깥으로 튀어나온 물리적 변형 표현
**근거**:
- 실제 핸드팬 제작 시 4방향에서 금속을 잡아당김
- 장력이 균등하지 않고 앵커 방향으로 집중
- 360° 방향성을 선(line)이 아닌 형태(shape)로 표현
**구현**: 가우시안 분포 기반 bulge 함수, 12% 돌출
**효과**:
- **물리적 정확성 극대화**: 실제 악기의 림 변형 반영
- **시각적 단순화**: 24개 선 → 형태 변형으로 대체
- **방향성 강조**: 4방향 주축이 명확히 드러남

#### 6-1. 장력 림 Inward Bulge 전환 (v2.3)
**날짜**: 2025-11-16
**문제**: v2.2의 바깥 돌출(+12%)이 "삐져나왔다"로 보임
**결정**: Inward bulge로 전환 (-4%), sigma 확대 (π/64 → π/24)
**효과**: "잡아당긴" 느낌, 완만한 납작 구간

#### 6-2. 장력 림 완전 제거 (v2.4) 🔥🔥🔥
**날짜**: 2025-11-16
**문제**: Inward/outward bulge 모두 경계선 변형의 물리적 의미가 불명확
**피드백**: "타원 경계선 자체를 변형하는 것은 의미가 없다"
**결정**: **장력 림 기능 완전 비활성화** (`enabled: false`)
**근거**:
- **경계선 순수성**: 타원 경계는 순수한 타원형으로 유지
- **물리적 명확성**: 장력 효과는 내부 레이어로만 표현
- **시각적 일관성**: 경계선 변형 없이도 충분히 장력 표현 가능
**효과**:
- 타원 경계선 = 완벽한 타원형 (어디도 눌리거나 찌그러지지 않음)
- 장력 표현 = 내부 효과만 (등고선, 장력선, 섀도우, 벨트 등)
- **설계 철학 명확화**: 경계 vs 내부 효과 구분

#### 7. 등고선 극단적 림 집중 (v2.2)
**날짜**: 2025-11-16
**문제**: v2.1의 [0.70, 0.82, 0.90, 0.97]도 여전히 중앙 70%에 정보
**결정**: [0.88, 0.94, 0.98]로 림 88-98% 구간만 집중, 3개로 축소
**효과**:
- 중앙 0-88% 완전히 깨끗
- **"림 10% 구간에 모든 정보 집중"** 극대화
- 시각적 명확성 향상

#### 8. 방사형 장력선 → 주축 장력선 (v2.2) 🔥
**날짜**: 2025-11-16
**문제**: 24개 전방향 장력선이 장력 림과 중복
**결정**: 4개 주축(Octave/Tonic/Fifth)만 표시
**근거**:
- 장력 림이 이미 360° 변형으로 전방향 장력 표현
- 선은 "핵심 음정 방향만" 강조하는 역할로 변경
- 시각적 정보 밀도 감소 필요
**효과**:
- 시각적 복잡도 대폭 감소 (24개 → 4개)
- 주요 음정 방향 명확히 강조
- 장력 림 bulge와 방향 일치 → 통합된 시스템

#### 9. 벡터 필드 비활성화 (v2.2)
**날짜**: 2025-11-16
**문제**: 24개 섹터 패턴이 등고선/장력선과 시각적 충돌
**결정**: `enabled: false`로 비활성화
**근거**:
- 장력 림 bulge가 방향성 표현 대체
- 시각 정보 과다로 림 집중 약화
- 주축 강조 전략과 불일치
**효과**: 시각적 단순화, 림 집중 전략 강화

#### 10. 림 앵커 틱 축소 (v2.2)
**날짜**: 2025-11-16
**문제**: 24개 틱이 장력 림과 중복
**결정**: 4개 주축 방향만 표시
**효과**: 주축 장력선과 일치, 시각적 일관성

#### 11. 림 고장력 링 추가 (v2.2)
**날짜**: 2025-11-16
**목적**: 림 최외각(96-100%)의 최대 장력 구간 강조
**근거**:
- 장력 벨트(94-100%)보다 더 바깥쪽 강조 필요
- 장력 림 path에 적용하여 변형된 형태 따라감
**효과**: 최대 장력 지점 명확 표시, 입체감 극대화

#### 11-1. 림 고장력 링 → 림 눌림 링 재정의 (v2.5)
**날짜**: 2025-11-16
**문제**: v2.4에서 밝은 링으로 "최대 장력"을 표현했으나, "눌린 림" 의도 불명확
**결정**: 어두운 쉐도우 링으로 재정의
**변경 사항**:
- 색상: `#ffffff` → `#020617` (밝음 → 어두움)
- opacity: 0.8 → 0.35 (강한 빛남 → 적당한 쉐도우)
- 이름: Rim Stress Ring → Rim Press Ring
**근거**:
- 중앙 돔 쉐이딩(밝음)과 대비하여 림의 눌림 명확화
- 장력 벨트(밝은 띠, 94-98%)와 림 눌림 링(어두운 쉐도우, 96-100%)의 조합
- "막이 고정되는 안쪽 경계" + "눌린 림 자리" = 경계 강조
**효과**: "눌린 림" 효과 극대화, 중앙 돔과의 대비 명확

#### 12. 외부 방사형 화살표 재활성화 (v2.5)
**날짜**: 2025-11-16
**문제**: v2.1-v2.4에서 비활성화된 외부 화살표 (360개)
**피드백**: "톤필드 바깥에 존재하던 바깥방향 화살표를 다시 보이게 해줘"
**결정**: `false &&` 조건 제거하여 재활성화
**변경 사항**:
- 코드: `{false && dirs.map...}` → `{dirs.map...}`
- 코멘트: "비활성화" → "v2.5 재활성화"
**근거**:
- 내부 장력선(12개)이 추가되어 내부 방향성 확보됨
- 외부 화살표는 리플 엣지와 조합하여 "외부 응력 전파" 표현
- 내부(장력선) + 외부(화살표) 균형으로 완전한 장력 시스템 시각화
**효과**:
- 톤필드에서 외부로 전파되는 에너지 시각화
- 리플 엣지(파동)와 화살표(방향)의 상호보완
- 활성 레이어: 10개 → 11개

---

## 📊 코드 구조 및 라인 맵

### Configuration Objects

| 객체명 | 라인 | 파라미터 수 | 설명 |
|--------|------|-------------|------|
| `CONTOUR_CONFIG` | 156-173 | 7개 | 등고선 (림 집중) |
| `RADIAL_TENSION_LINES_CONFIG` | 178-197 | 9개 | 장력선 (24개, 림 강조) |
| `MEMBRANE_SHADING_CONFIG` | 200-220 | 10개 | 멤브레인 (대비 강화) |
| `VECTOR_FIELD_CONFIG` | 223-241 | 7개 | 벡터 필드 |
| `INWARD_SHADOW_CONFIG` | 243-257 | 5개 | 인워드 섀도우 (확장) |
| `RIPPLE_EDGE_CONFIG` | 260-280 | 5개 | 리플 엣지 |
| `TENSION_BELT_CONFIG` | 282-303 | 10개 | 🆕 장력 벨트 |
| `RIM_ANCHOR_CONFIG` | 306-320 | 5개 | 🆕 림 앵커 |

**총 CONFIG 파라미터**: 58개

### SVG Definitions

| 요소 | 라인 | 타입 | 설명 |
|------|------|------|------|
| `rippleBlur` | 478-481 | filter | 리플 블러 필터 |
| `tension-line-stroke-gradient` | 531-555 | radialGradient | 장력선 (림 강조) |
| `membrane-shading-gradient` | 557-590 | linearGradient | 멤브레인 (4-stop) |
| `inward-shadow-gradient` | 616-635 | radialGradient | 인워드 섀도우 |
| `tension-belt-gradient` | 637-667 | radialGradient | 🆕 장력 벨트 (4-stop) |

### Rendering Layers

| 레이어 | 라인 | 요소 타입 | 개수 | 설명 |
|--------|------|-----------|------|------|
| Vector Field | 745-808 | `<path>` | 24개 | 섹터 렌더링 |
| Membrane Shading | 835-842 | `<path>` | 1개 | 도넛 path |
| Contour Lines | 845-861 | `<ellipse>` | 4개 | 등고선 |
| Tension Lines | 863-891 | `<line>` | 24개 | 장력선 |
| Inward Shadow | 937-946 | `<path>` | 1개 | 그림자 |
| Tension Belt | 948-957 | `<path>` | 1개 | 🆕 밝은 띠 |
| Rim Anchors | 976-1003 | `<line>` | 24개 | 🆕 틱 마크 |
| Ripple Edge | 760-785 | `<ellipse>` | 2개 | 리플 |
| ❌ External Arrows | 1105-1114 | `<path>` | 0개 | 비활성화 |

**총 렌더링 요소**: 81개 (화살표 제외)

---

## 📞 외부 전문가 분석 요청 사항

### 검토 항목 및 평가 기준

#### 1. 물리적 타당성 (★★★★★)
**질문**:
- 림 집중 장력 분포가 실제 핸드팬 물리와 일치하는가?
- 직선 장력선이 팽팽한 금속막을 정확히 표현하는가?
- 24개 앵커 포인트 분포가 실제 제작 방식과 부합하는가?
- 외부 화살표 비활성화 결정이 물리적으로 타당한가?

**평가 지표**:
- 장력 분포 정확도
- 물리 법칙 준수도
- 실제 악기와의 일치도

#### 2. 시각적 효과성 (★★★★☆)
**질문**:
- "림에 장력 집중" 의도가 명확히 전달되는가?
- 8개 레이어의 상호작용이 조화로운가?
- 정보 밀도 그라데이션(0-70%-90-100%)이 자연스러운가?
- 장력 벨트와 림 앵커가 효과적인가?

**평가 지표**:
- 직관성
- 가독성
- 시각적 명확성

#### 3. 파라미터 최적성 (★★★★☆)
**질문**:
- 등고선 비율 [0.70, 0.82, 0.90, 0.97]이 적절한가?
- 장력선 24개가 과하지/부족하지 않은가?
- 장력 벨트 opacity 0.55가 적절한가?
- 림 앵커 길이 8px가 적절한가?

**평가 지표**:
- 밀도 적정성
- 대비 강도
- 세부 가시성

#### 4. 색상 및 접근성 (★★★☆☆)
**질문**:
- Teal/Green 계열 색상이 장력 표현에 적합한가?
- 색상 대비가 WCAG 2.1 AA/AAA 기준을 만족하는가?
- 색각 이상자가 구분 가능한가?
- 다크 모드 대응이 필요한가?

**평가 지표**:
- 대비비 (4.5:1 이상)
- 색각 이상자 가시성
- 다중 환경 호환성

#### 5. 확장 가능성 (★★★★☆)
**질문**:
- 다른 크기/형태의 톤필드에 적용 가능한가?
- 실시간 장력 변화 시뮬레이션으로 확장 가능한가?
- 애니메이션 추가 시 성능 영향은?
- 파라미터 외부 제어(API) 가능한가?

**평가 지표**:
- 재사용성
- 확장성
- 유지보수성

#### 6. 성능 및 최적화 (★★★☆☆)
**질문**:
- SVG 복잡도(81 요소)가 허용 범위인가?
- 렌더링 성능이 적절한가? (60fps 유지?)
- 추가 최적화 기회가 있는가? (예: CSS, Canvas)
- 메모리 사용량은?

**평가 지표**:
- 렌더링 시간
- 메모리 사용량
- 확장 시 성능 저하

---

## 📈 향후 개선 방향

### Phase 3: 동적 시각화 (우선순위: 높음)

**1. 실시간 장력 시뮬레이션**
```typescript
interface TensionSimulation {
  strikeForce: number;        // 타격 강도 (0-1)
  frequency: number;          // 진동 주파수 (Hz)
  dampingFactor: number;      // 감쇠 계수
  timeElapsed: number;        // 경과 시간 (ms)
}

// 장력 분포 실시간 계산
const calculateTensionDistribution = (
  params: TensionSimulation
): TensionField => {
  // 유한요소해석(FEA) 기반 계산
  // 등고선 ratios 동적 조정
  // 장력선 opacity 동적 변경
};
```

**2. 주파수별 진동 모드**
- 1st mode: 기본 톤 (가장 간단한 패턴)
- 2nd mode: 옥타브 (더 복잡한 패턴)
- 3rd mode: 배음 (고차 패턴)

**3. 재료 특성 반영**
- 강철 vs 니트라이드 강철
- 두께 변화에 따른 장력 분포
- 템퍼링 효과

### Phase 4: 인터랙티브 요소 (우선순위: 중간)

**1. 호버 효과**
```typescript
// 특정 장력선 호버 시
onTensionLineHover = (lineIndex: number) => {
  // 해당 장력선 opacity 0.80 → 1.0
  // 대응하는 림 앵커 강조
  // 툴팁 표시: "장력 방향 #15 (157.5°)"
};
```

**2. 클릭 정보**
```typescript
// 특정 지점 클릭 시
onPointClick = (x: number, y: number) => {
  const tension = calculateTensionAt(x, y);
  showTooltip({
    position: { x, y },
    data: {
      tension: `${tension.toFixed(2)} N/m`,
      distance: `${distanceFromCenter}%`,
      angle: `${angle.toFixed(1)}°`
    }
  });
};
```

**3. 드래그 조정**
- 딤플 위치 드래그
- 타원 크기 조정
- 앵커 개수 변경 (16/24/32)

### Phase 5: 애니메이션 (우선순위: 낮음)

**1. 장력선 펄스**
```typescript
// 타격 시뮬레이션
const animateTensionPulse = () => {
  // 중심에서 시작
  // 림으로 전파 (0.5초)
  // 반사파 (0.3초)
  // 감쇠 (1초)
};
```

**2. 리플 엣지 확산**
```typescript
// 외부 전파 애니메이션
const animateRippleExpansion = () => {
  // factor 1.03 → 1.10 (2초)
  // opacity 0.40 → 0.0 (페이드아웃)
};
```

**3. 벡터 필드 회전**
```typescript
// 진동 모드 시각화
const rotateVectorField = (frequency: number) => {
  // 섹터 밝기 패턴 회전
  // 속도 = frequency * 0.1 (rpm)
};
```

### Phase 6: 물리 엔진 연동 (우선순위: 연구)

**1. FEA 데이터 통합**
- COMSOL/ANSYS 출력 파싱
- 메시 데이터 → SVG 변환
- 실시간 응력 분포 표시

**2. 음향 연동**
- Web Audio API 통합
- 주파수 스펙트럼 → 시각화
- 실시간 사운드 분석

---

## 📋 기술 통계

### 코드 복잡도

| 지표 | 값 | 비고 |
|------|-----|------|
| 총 라인 수 | ~1200 | TypeScript + JSX |
| CONFIG 객체 | 8개 | 58개 파라미터 |
| SVG 정의 | 5개 | gradient + filter |
| 렌더링 요소 | 81개 | 화살표 제외 |
| 활성 레이어 | 8개 | 등고선~리플 |
| 비활성 요소 | 1개 | 외부 화살표 |

### 렌더링 성능 (예상)

| 항목 | 값 | 측정 방법 |
|------|-----|-----------|
| 초기 렌더링 | <50ms | Chrome DevTools |
| 리페인트 | <16ms | 60fps 유지 |
| 메모리 사용 | <5MB | Heap snapshot |
| DOM 노드 | ~100개 | Elements 탭 |

### 파라미터 통계

| 레이어 | 파라미터 수 | 조정 가능 | 의존성 |
|--------|-------------|-----------|--------|
| 등고선 | 7 | 7 | 독립 |
| 장력선 | 9 | 9 | 독립 |
| 멤브레인 | 10 | 10 | 독립 |
| 벡터 필드 | 7 | 7 | 독립 |
| 인워드 | 5 | 5 | 독립 |
| 리플 | 5 | 5 | 독립 |
| 장력 벨트 | 10 | 10 | 독립 |
| 림 앵커 | 5 | 5 | 독립 |
| **합계** | **58** | **58** | **0** |

---

## 🏆 핵심 성과 지표 (KPI)

### 물리적 정확성
- ✅ 직선 장력선 (팽팽함 표현)
- ✅ 림 집중 분포 (물리 법칙)
- ✅ 24개 균등 고정점 (실제 구조)
- ✅ 외부 화살표 제거 (오인 방지)

### 시각적 명확성
- ✅ 중앙 70% 깨끗 (정보 밀도 0%)
- ✅ 림 90-100% 집중 (정보 밀도 100%)
- ✅ 8개 레이어 조화 (계층 최적화)
- ✅ 경계 하이라이트 (장력 벨트)

### 기술적 완성도
- ✅ 58개 파라미터 독립 제어
- ✅ 81개 요소 효율 렌더링
- ✅ 0 의존성 (완전 독립)
- ✅ 실시간 조정 가능

---

**문서 버전**: 2.5 (중앙 돔 쉐이딩 + 내부 장력선)
**최종 업데이트**: 2025-11-16
**변경 이력**:
- v1.0: 초기 6개 레이어 구현
- v2.0: 림 집중 최적화 + 2개 레이어 추가 (장력 벨트, 림 앵커)
- v2.1: 외부 화살표 비활성화 + 24개 방사형 장력선 구현
- v2.2: 장력 림(Tension Rim) 추가 (outward bulge +12%)
- v2.3: 장력 림 inward bulge 전환 (-4%)
- v2.4: 🔥 **장력 림 완전 제거** + 순수 타원 경계 유지
  - ❌ 장력 림 비활성화: 경계선 변형은 물리적 의미 불명확
  - ✅ 타원 경계선: 완벽한 타원형 유지
  - ✅ 장력 표현: 내부 레이어만으로 충분 (9개 레이어)
  - 방사형 장력선 24개 → 주축 장력선 4개
  - 등고선 극단적 림 집중 [0.88, 0.94, 0.98]
  - 림 고장력 링 추가 (96-100%)
  - 인워드 섀도우 림 집중 강화 (85-100%)
- v2.5: ⚡⚡ **중앙 돔 쉐이딩 + 내부 장력선 + 림 강조 + 외부 화살표 재활성화** (제안 2+3+보너스)
  - ⚡⚡ 멤브레인 쉐이딩 → 중앙 돔 쉐이딩 (linearGradient → radialGradient)
    - 효과: 중앙 돔 볼록 (0-25%) + 림 눌림 (55-100%)
    - 물리적 의미: "중앙에서 펴지는 힘" 명확화
  - ⚡ 내부 장력선 추가 (Inner Stretch Lines, 12개)
    - 림(95%) → 중앙(40%) 반쯤만 들어오는 짧은 선
    - 효과: "막의 결" 표현, 당기는 뉘앙스 복원
    - 매우 얇고 옅게 (0.6px, opacity 0.18)
  - ⚡ 장력 벨트 재조정 (94-98%, 안쪽 경계 강조)
    - 94%: opacity 0.65 (밝은 띠)
    - 98%: opacity 0.10 (페이드아웃)
    - 효과: "막이 림에 고정되는 바로 안쪽 밝은 띠"
  - ⚡ 림 고장력 링 → 림 눌림 링 (Rim Press Ring)
    - 96-100%: #020617 opacity 0.35→0.0 (어두운 쉐도우)
    - 효과: "눌린 림" 명확화, 어두운 그림자로 눌림 강조
  - ⚡ 외부 방사형 화살표 재활성화 (External Radial Arrows)
    - 360개 외부 화살표 복원 (`false &&` 조건 제거)
    - 효과: 외부 응력 전파 시각화, 리플 엣지와 조합
  - ✅ 활성 레이어: 9개 → 11개

**총 개발 단계**: 7단계 (v1.0 → v2.5)
**총 커밋 수**: 7회 (v2.5 미커밋)
**핵심 철학**: 경계선 순수성 유지, 내부 효과로만 장력 표현
**작성자**: Claude Code with SuperClaude Framework
